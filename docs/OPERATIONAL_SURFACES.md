# orv Operational Surfaces

이 문서는 현재 CLI, editor/LSP/DAP, build/deploy, DB 운영 surface를 추적한다. 핵심 크레이트 구조와 데이터 흐름은 [ARCHITECTURE.md](ARCHITECTURE.md)에 둔다.

## CLI Surface

현재 `orv-cli`는 프로젝트 scaffold, 로드/해석/분석/실행, graph/origin 출력, editor/LSP/DAP bootstrap, build artifact 생성/검증/실행, lockfile 생성/검증, DB snapshot/migration workflow를 오케스트레이션한다.

주요 command:

- `orv init <dir> --name <name> [--template basic|shop]`
- `orv run/dev/check/dump/origins/graph/test`
- `orv editor snapshot/reveal/runtime/debug/run-debug/export/trace/trace-stream`
- `orv lsp snapshot/reveal/serve --stdio`
- `orv dap serve --stdio`
- `orv build <file-or-orv.toml> --out <dir> [--prod]`
- `orv add/remove`
- `orv lock [dir-or-orv.toml] [--check]`
- `orv fetch [dir-or-orv.toml] [--out <dir>]`
- `orv workspace new <member> [--root <dir>] [--name <name>]`
- `orv workspace graph [root] [--view] [--out <dir>]`
- `orv workspace lock [root] [--out <dir>]`
- `orv workspace fetch [root] [--out <dir>]`
- `orv workspace build [root] [--out <dir>] [--prod] [--incremental]`
- `orv verify-build/verify-artifact/check-artifact/check-build`
- `orv benchmark-report <dir> [--require-pass]`
- `orv run-artifact/run-build/reveal`
- `orv db plan/verify/apply/migrate/rollback/backup/restore/recover/archive/squash`

Source-entry commands accept a single `.orv` file, an `orv.toml` with `[project].entry`, or a project directory containing `orv.toml`.

Reference HTTP routes now support named-schema request bindings: `@body: T`, `@query: T`, and `@form: T` run the same runtime validators as `T.parse(...)`, expose normalized request-state values to the remaining handler body, and short-circuit invalid input with a 400 `{ schema_version: 1, kind: "orv.validation.error", error: "validation_failed", fields: [...] }` response.

`orv init <dir> --template shop` writes the reference shop starter with editable `@design` color/spacing/typography tokens on the home shell, an end-to-end `ProductInput.badge` field path that flows through the product form, `POST /products`, customer catalog, admin catalog, and generated smoke body checks, `GET /` browser checkout forms, typed `@body: T` schema bindings on browser mutation routes, a customer-facing browser `GET /catalog` storefront catalog, browser `GET /cart` cart view, browser `GET /account/sessions` active session view protected by `@session required`, signup/login forms that store Argon2 `passwordHash` values with `hash.password` and verify them with `hash.verify`, a seeded reference admin member for local smoke sessions, `@Auth required role="admin"` protected admin dashboard/read models for catalog/order/payment/shipment/webhook/audit data and persistence paths, an env-overridable `SHOP_DATABASE_URL` DB adapter that defaults to SQLite-backed `data/shop.sqlite`, env-overridable payment/shipping adapters that default to file-backed commerce record logs under `data/`, reference session/role cookies for successful member login, reference CSRF hidden-token wiring for browser mutation forms, reference rate limits for login/checkout/webhook routes, a reference `POST /webhooks/stripe` signature verification and duplicate event endpoint, and generated build/deploy/native launcher guidance including the generated smoke-output artifact and required benchmark smoke markers.

Build, server runtime, deploy, and native plan artifacts expose those reference security requirements as shared `runtime_features`: `auth_roles`, `session_cookies`, `csrf_protection`, and `rate_limit`. Route descriptors also carry per-route `policies[]` entries for source-backed `auth`, `session`, `csrf`, and explicit `rateLimit` domains plus built-in `rate_limit` defaults, and `orv reveal` includes those policies in production route payloads. Route handlers can mark intentional CSRF bypasses with `@csrf exempt`, override rate-limit defaults with `@rateLimit key=@body.memberId limit=2 window="1m"`, or disable a built-in hotspot limit with `@rateLimit exempt`; the reference runtime applies those checks before handler execution. `orv verify-build` checks runtime feature equality and source origin containment for those route policies, so drift in the advertised security surface fails before deploy smoke or native launcher metadata is trusted.

`orv graph <file> --view --out <dir>` writes `graph.json` and a static `index.html` ProjectGraph view with source/semantic depth stats, node/edge visualization, node search/kind filtering, and origin rows.

`orv dev --hmr` writes `dev/session.json`, `dev/transport.json`, and `dev/hmr-client.js` for the reference EventSource HMR browser transport; `orv dev --watch` writes `dev/watch.json`; `orv dev --watch-loop [--watch-iterations <n>]` runs the poll-loop build/verify/run path while writing `dev/events.json`; and `orv dev --hmr --serve [--serve-port <port>]` starts the reference HTTP/1 HMR endpoint with `dev/server.json`, `/__orv/hmr/session`, and `/__orv/hmr/events`.

`orv lock [dir-or-orv.toml]` reads `[project]`, `[dependencies]`, and `[dev-dependencies]` from `orv.toml`, writes deterministic JSON `orv.lock` entries sorted by package name, preserves registry/path sources, preserves optional `auth_token_env` names without storing secret tokens, resolves exact semver versions including prerelease/build metadata directly, resolves `*`, `x`, segment wildcards, caret ranges, tilde ranges, whitespace-AND comparator ranges, and `||` disjunction ranges from local/file/HTTP/HTTPS registry `index.json` into exact locked versions, preserves the original range as `requested_version`, and adds stable `fnv1a64` checksums. HTTP/HTTPS registry index requests use `Authorization: Bearer <token>` when `auth_token_env` is present. `--check` compares the existing lockfile without writing.

`orv fetch [dir-or-orv.toml] --out <dir>` verifies that `orv.lock` matches `orv.toml`, materializes path dependencies, local/file registry dependencies, and HTTP/HTTPS registry `/<package>/<version>/source-bundle.json` artifacts into source-bundle cache artifacts, sends `Authorization: Bearer <token>` for registry entries with `auth_token_env`, and writes `deps-manifest.json`.

`orv add <pkg> <version> [--dev] [--path <path>] [--registry <url>] [--manifest <dir-or-orv.toml>]` edits the selected dependency section and regenerates `orv.lock`; `orv remove <pkg> [--dev] [--manifest <dir-or-orv.toml>]` removes from the selected section and regenerates the lockfile.

`orv workspace new <member> [--root <dir>] [--name <name>]` creates a basic member project and records the relative member path in root `orv.toml` `[workspace].members` with resolver `2`.

`orv workspace graph [root] [--view] [--out <dir>]` reads root `[workspace].members`, loads each member entry through the shared ProjectGraph pipeline, records member graphs/files/dependencies, and emits path dependency edges between workspace members. Workspace path dependency edges include dependency package/section, target member name/version, requested version when present, and reject requested versions that do not match the target member version. With `--out`, it writes `workspace-graph.json`; with `--view`, it writes `workspace-graph.json` plus a static `index.html` workspace member/dependency graph view with member/edge search filtering.

`orv workspace lock [root] --out <dir>` reads the same workspace graph, orders members dependency-first from path dependency edges, writes per-member lockfiles under `members/<member>/orv.lock`, and emits `workspace-lock.json` with member project metadata, dependency lists, path dependency edges, lock order, and package counts without mutating member source directories.

`orv workspace fetch [root] --out <dir>` writes the same workspace graph and workspace lock artifacts, then materializes each member's lockfile into `members/<member>/deps/deps-manifest.json` and per-member dependency source-bundle caches. It emits `workspace-fetch.json` with dependency-first fetch order, member dependency manifests, and total package counts.

`orv workspace build [root] --out <dir> [--prod] [--incremental]` reuses the normal build pipeline for every workspace member, orders member builds dependency-first from path dependency edges, verifies each member build directory, writes member artifacts under `members/<member>`, and emits `workspace-build.json` plus `workspace-graph.json` as the top-level workspace build contract. With `--incremental`, unchanged member source-bundle input hashes are skipped when the previous verified build is still valid; rebuilt dependencies force dependent members to rebuild.

## Editor And LSP

`orv editor snapshot <file>` emits first-party editor bootstrap JSON with diagnostics, ProjectGraph, Files/Routes/Schema/Domains panel inputs, and source-hash watch sources. `orv editor reveal <dir> <origin-id>` maps build artifact origins to editor focus/source/production navigation payloads, including static page/client bundle targets for `@html` projection origins and containing route/native-server targets for route-local HTML origins. `orv editor runtime <file>` reuses DAP trace/runtime helpers for runtime inspection pane JSON. `orv editor debug <file> --breakpoint <path>:<line> --function-breakpoint <name> --data-breakpoint <local-name> --exception-filter <orv.diagnostics|orv.runtime> --control <continue|pause|reverse-continue|next|step-back|step-in|step-in-targets|step-out|restart-frame|restart|terminate|terminate-threads|disconnect> --watch-expression <expr>` runs an initialize/setExceptionBreakpoints/setFunctionBreakpoints/live-launch/loadedSources/source/setBreakpoints/dataBreakpointInfo/setDataBreakpoints/control/stackTrace/evaluate sequence over the same Content-Length DAP transport and emits the response/event frames for native editor wiring; repeated `--control` values run in order inside one session and repeated watch expressions evaluate after the controls. `orv editor run-debug <state-or-runner.json|build-dir> --breakpoint <path>:<line> --function-breakpoint <name> --data-breakpoint <local-name> --exception-filter <orv.diagnostics|orv.runtime> --control <...> --watch-expression <expr>` reads an exported `debug.session_runner`, `debug/session-runner.json` standalone artifact, or build directory with `source-bundle.json`, applies requested source/function/data breakpoints plus exception filters, replays source inventory capture, controls, and watch evaluations through one DAP session for the recorded or bundled program, emits runner result JSON for native host execution, writes that result to `debug/session-result.json` plus a rendered `debug/session-result.html` panel when the runner artifact declares the result path, and includes `panels.debug.session_summary`, `panels.debug.source_bundle`, `panels.debug.source_navigation`, `panels.debug.production_summary`, plus selected-frame/scope/locals/stack/control/breakpoint/function-breakpoint/data-breakpoint/exception-filter/watch-expression/loaded-source/source-snapshot/event summaries for native UI refresh. `orv editor export <file> --out <dir>` writes `state.json`, `debug/session-runner.json`, `native-host.json`, plus a static `index.html` shell with panel lists, ProjectGraph visualization, runtime frame inspection, DAP adapter launch/live/attach configuration wiring, live-control request payloads, native-host `session_runner` command metadata, DAP loadedSources/source request inventory with SHA256 source checksums, executable breakpoint source lines, ProjectGraph function breakpoint rows, runtime local data breakpoint rows, and exception filter rows with runner commands, debug result artifact metadata, trace stream runner command metadata, native-host trace summary/frame navigation/status-filter inventory, and optional trace navigation. Exports also write a focused `runtime/panel.html` runtime companion, and trace-enabled exports write `trace/panel.html`. When `--build <dir>` is supplied, the export state, `native-host.json` `production` block, static shell, and `production/panel.html` companion include a Production panel summarizing checked `source-bundle.json`/`project-graph.json`/`origin-map.json` graph contracts, client bundle targets with manifest capabilities, native-server plan/route/blocker targets, static page verification targets, preflight command and route-policy counts, DB, and commerce adapter contract artifacts. `native-host.json` is the native editor host manifest: it points at the shell/state/debug runner/result JSON/result HTML/runtime panel/production panel/trace panel artifacts, includes `panels[]` as an ordered native panel inventory, and carries runtime frame/panel contract, DAP adapter command, DAP source inventory, runner command, debug panel contract, result artifact panel contract, breakpoint/function-breakpoint/data-breakpoint/exception-filter/watch-expression argument contracts, trace stream runner contract, trace summary, per-frame trace source/production navigation, production graph/source/origin contract summary, production client/native/static bundle summary/contracts, production preflight command/route-policy/adapter summary/contracts/panel contract, and trace/debug capabilities. `orv editor trace <dir> --trace <trace.json>` maps captured request frames back to route source/production navigation and, when `response_origin_id` is present, also attaches response source/production navigation for the selected `@respond`; it embeds the `/__orv/trace/events` open-ended EventSource trace transport URL when a server artifact has a stable listen endpoint. `orv editor trace-stream <dir> --events <trace-events.sse>` consumes a native-host EventSource body, extracts `orv:trace` snapshots plus `orv:trace.frame` deltas, and emits normalized editor trace stream JSON with latest trace payload and transport metadata.

Editor DAP control, breakpoint, function-breakpoint, data-breakpoint, exception-filter, source, and watch-expression commands are now first-class in the export contract: every exported control carries the exact `orv editor run-debug debug/session-runner.json --control <name>` runner command, every executable breakpoint row carries a `--breakpoint <path>:<line> --control continue` runner command plus `setBreakpoints` request preview, every ProjectGraph function/define row carries a `--function-breakpoint <name> --control continue` runner command plus `setFunctionBreakpoints` request preview, every runtime local row carries a `--data-breakpoint <local-name> --control continue` runner command plus `dataBreakpointInfo`/`setDataBreakpoints` request previews, every exception filter row carries an `--exception-filter <filter> --control continue` runner command plus `setExceptionBreakpoints` request preview, exported source inventory carries `loadedSources` and per-source `source` request previews with SHA256 checksums, `native-host.json` mirrors control commands, DAP source inventory, breakpoint/function-breakpoint/data-breakpoint/exception-filter command inventory, breakpoint/function-breakpoint/data-breakpoint/exception-filter/watch-expression argument formats, a `debug.panel_contract` for host-side configuration/source/control/breakpoint/function-breakpoint/data-breakpoint/exception-filter/watch/result panels, the stable `debug/session-result.json` result artifact path with `panels.debug.session_summary`, `panels.debug.source_bundle`, `panels.debug.source_navigation`, `panels.debug.production_summary`, `panels.debug.scopes`, locals, project variables, `panels.debug.function_breakpoints`, `panels.debug.data_breakpoints`, `panels.debug.exception_filters`, `panels.debug.watch_expressions`, `panels.debug.loaded_sources`, `panels.debug.source_snapshots`, and a `panel_contract` for native UI sections, and the rendered `debug/session-result.html` companion panel, and the static shell renders the selected runner command plus result artifact metadata next to the DAP request payload. Build-backed exports also copy the raw DAP launch source-bundle metadata, production graph-contract, preflight benchmark evidence, and native/static/client/smoke summary counters into `debug.production_context`, the standalone `debug/session-runner.json`, `native-host.json` debug metadata, `orv editor run-debug` runner results, `panels.debug.source_bundle`, and `panels.debug.production_summary` in the debug result panel so DAP controls keep the same source-bundle/project-graph/origin-map and smoke marker context even when the runner is launched outside `state.json`. Production exports also mirror the build graph contract through `production.graph_contract`, including source-bundle file hashes, ProjectGraph node/semantic counts, origin-map entry/edge counts, artifact hashes, and `production.summary` counters, so native hosts do not need to infer the shared source/origin spine from client or adapter payloads. The same native-host summary now counts native-server plan/routes/blockers, static page verification, and preflight smoke evidence summaries that are present, missing, or present with missing marker gaps; the Production panel renders those native/static/smoke metrics next to the graph/preflight counters. `deploy/preflight.json` names the same graph artifacts, and generated deploy smoke tests run from the build directory, check those artifacts, execute `orv verify-build .`, assert one cached `orv editor run-debug . --control next` output exposes `panels.debug.source_bundle` path/file-count/hash metadata plus the smoke required-marker list, and assert CLI/editor/LSP route reveal payloads carry native-server target/route summary counters and the same smoke required-marker contract before route checks proceed, so production smoke also fails on source-bundle/project-graph/origin-map, DAP source-bundle, smoke marker, or native summary drift.

Trace transport is also mirrored for native hosts: when an editor export includes trace state and the build has a stable server listen endpoint, `native-host.json` includes `trace.transport` with the `/__orv/trace/events` EventSource URL plus `trace.stream_runner.command` for `orv editor trace-stream <build-dir> --events trace/events.sse`, `trace.status_filters`, and `trace.frames[*]` request summary/source/production navigation entries. Each source-backed trace frame also exposes flattened `source`, `production`, and `reveal_command` fields so native hosts can open `orv editor reveal <build-dir> <origin-id>` without reparsing nested navigation state; response-backed frames additionally expose `response_source`, `response_production`, and `response_reveal_command` from `response_origin_id`. Trace-enabled exports write `trace/panel.html`, mirror it as `artifacts.trace_panel_html`, `trace.panel_html_path`, and `trace.panel_artifact`, and include that artifact in the trace panel contract while the static shell renders the transport and stream runner panes.

`orv lsp serve --stdio` currently handles:

- lifecycle: `initialize`, `shutdown`, notifications, unknown-method errors
- document/project: `textDocument/documentSymbol`, `workspace/symbol`, `textDocument/diagnostic`, `workspace/diagnostic`
- editor affordances: `textDocument/codeLens`, `textDocument/codeAction`, `workspace/executeCommand`, `textDocument/documentLink`, `textDocument/documentColor`, `textDocument/colorPresentation`, `textDocument/foldingRange`, `textDocument/selectionRange`, `textDocument/semanticTokens/full`, `textDocument/linkedEditingRange`, `textDocument/formatting`, `textDocument/rangeFormatting`, `textDocument/onTypeFormatting`
- navigation: `textDocument/definition`, `textDocument/declaration`, `textDocument/typeDefinition`, `textDocument/implementation`, `textDocument/moniker`
- hierarchy: `textDocument/prepareCallHierarchy`, `callHierarchy/incomingCalls`, `callHierarchy/outgoingCalls`, `textDocument/prepareTypeHierarchy`, `typeHierarchy/supertypes`, `typeHierarchy/subtypes`
- editing/introspection: `textDocument/references`, `textDocument/documentHighlight`, `textDocument/prepareRename`, `textDocument/rename`, `textDocument/hover`, `textDocument/signatureHelp`, `textDocument/inlayHint`, `textDocument/completion` with project symbols, language/directive/route-method snippets, `@body`/`@param`/`@query`/`@env` dot completions learned from open project source, `@param` names inferred from route `:param` path segments, and hover/reference/highlight support for `@body.*`/`@param.*`/`@query.*`/`@env.*` references

Code actions include diagnostic reveal commands plus edit quickfixes for incomplete `@route` heads, inserting default HTTP method/path text where the parser diagnostic lands.

Identifier edit/introspection surfaces (`references`, `documentHighlight`, `linkedEditingRange`, `prepareRename`, and `rename`) reject reserved language tokens and lowercase built-in `@` directives, so editor actions cannot rewrite or spotlight syntax keywords and core route/server directives as user symbols. Domain field names under `@body.*`/`@param.*`/`@query.*`/`@env.*` have dedicated reference/highlight lookup and stay outside rename/linked-editing because they are request-shape keys, not local identifiers.

The LSP session keeps `textDocument/didOpen`, full-sync `textDocument/didChange`, and `textDocument/didSave` text buffers so later file URI requests can run against current editor content, and clears that cache on save-without-text or `textDocument/didClose`.

## Tree-sitter

`tree-sitter-orv/` ships the source grammar package for `.orv` editor integrations, including `grammar.js`, highlight/local query files, and a local smoke check. Generated parser C files and editor-specific bindings are intentionally not committed yet; run `npm run test:grammar` for the source grammar smoke check and `tree-sitter generate` from that directory when the Tree-sitter CLI is installed.

## DAP

`orv dap serve --stdio` reuses the same project loader and ProjectGraph as CLI/LSP. It supports initialize/cancel/launch/attach/configurationDone, source/function/instruction/data breakpoints, breakpoint/goto/step-in target discovery, exception info, threads/stackTrace/scopes/variables, variable/expression mutation, evaluate/completions, loadedSources/modules/source, source-frame disassemble/readMemory, continue/reverseContinue/goto/stepIn/stepBack/restartFrame, disconnect/terminate/terminateThreads, and stdio lifecycle/output events. DAP initialize advertises ORV-specific source-bundle launch support, and `launch.arguments.sourceBundle`/`source_bundle` can point at build `source-bundle.json` so raw DAP clients can rehydrate source snapshots even when the original source files are unavailable. Launch and restart responses expose `body.sourceBundle` with path, entry, file count, and stable hash, and `restart` preserves the previous build source-bundle path unless the caller supplies a different program. Snapshot-backed DAP Source objects include SHA256 source checksums for launch-time source integrity, `scopes`, `stepInTargets`, and `restartFrame` accept reported stack frame ids, current-frame scopes report `namedVariables`, `stackTrace` honors `startFrame`/`levels`, `variables` honors `start`/`count` plus `filter=named|indexed` for editor-side paging, and `setInstructionBreakpoints` verifies `orv:frame:N` pseudo-instruction references after launch so `continue` can stop on runtime frames even when a client drives the source-frame disassembly surface.

Runtime debug state comes from the reference runtime debug trace. Long-running `@server` launches start as a paused runtime frame and expose `continue`/`pause` event-loop control. `attachRuntime=true` or the standard `attach` request can attach a child `orv run <program>` process; `attachRuntimeMode="inProcess"` starts an attached server thread in the DAP process and preserves the same DB handle across top-level prefix statements, server boot body, and route handlers. In-process request frames are exposed through variables/evaluate/completions and can be flushed to `orv.production.trace` JSON via `runtimeRequestTracePath`, normal `orv run-artifact/run-build --trace <path>`, or `ORV_RUNTIME_REQUEST_TRACE_PATH`. When request trace capture is enabled, the reference HTTP runtime also serves an EventSource-compatible snapshot at `/__orv/trace/events`.

## Build And Deploy Artifacts

`orv build <file-or-orv.toml> --out <dir>` creates deterministic reference artifacts:

- `build-manifest.json`
- `bundle-plan.json`
- `origin-map.json`
- `project-graph.json`
- `source-bundle.json`
- `server/app.orv-runtime.json`
- `server/launch.json`
- `server/native-server.json`
- `server/runtime-image.json`
- `server/native/Dockerfile`
- `server/native/Cargo.toml`
- `server/native/main.rs`
- `server/native/routes.rs`
- `server/native/router.rs`
- `server/native/handlers.rs`
- `pages/index.html` for HTML-only zero-runtime entries
- `client/manifest.json`, `client/reactive-plan.json`, `client/app.js`, and a source-bound `client/app.wasm` with initial-render memory exports for interactive client entries

`orv build --prod` adds deploy artifacts:

- `deploy/manifest.json`
- `deploy/routes.json`
- `deploy/container.json`
- `deploy/Dockerfile`
- `deploy/compose.yaml`
- `deploy/env.example`
- `deploy/db-adapters.json`
- `deploy/commerce-adapters.json`
- `deploy/preflight.json`
- `deploy/benchmark-evidence.json`
- `deploy/smoke-test.sh`
- `deploy/README.md`
- `deploy/server.sh`

For zero-runtime static builds, `deploy/manifest.json` carries the checked static page target and `orv verify-build` requires it to match the bundle-plan `static_page` path and zero-runtime shape.

`server/native-server.json` is a native server binary contract, not a final optimized binary. Direct-lowered route artifacts are marked `direct_http` and no longer carry `native-codegen` or `native-runtime-image` blockers; dynamic fallback artifacts remain `planned` and keep `native-codegen`/`native-runtime-image` blockers. It records the current reference artifact, reference launcher, generated `server/native/Cargo.toml` launcher package, generated `server/native/main.rs` launcher source, generated `server/native/routes.rs` route table source, generated `server/native/router.rs` router dispatch source, generated `server/native/handlers.rs` handler source, `server/runtime-image.json` image plan, structured `commands.build`/`commands.run`, route/listen/runtime feature shape, and planned `server/app` HTTP/1 target. `server/runtime-image.json` records the reference runtime image, OCI image target, native binary path, generated `server/native/Dockerfile`, `docker build -f server/native/Dockerfile -t orv-native-server:latest .`, route/listen/runtime feature shape, and `image_planned` status for direct-lowered native launchers with no blockers; dynamic fallback launchers keep blockers until broader native lowering exists. `server/native/Cargo.toml`, `server/native/main.rs`, `server/native/Dockerfile`, `server/native/routes.rs`, `server/native/router.rs`, and `server/native/handlers.rs` form a generated Rust incremental native launcher package and image contract that checks the native plan and server artifact exist, links a typed route table with method/path/origin id constants, contained `@respond` origin ids, per-route security/traffic policy descriptors, a `:param`/`:rest*`-aware `orv_native_match_route`, route/query/body capture structs, `orv_native_param_value`/`orv_native_query_value`/`orv_native_body_json`/`orv_native_body_field_value` lookup helpers, route/response origin handler descriptor table, static literal `@respond` JSON body lowering, 1xx/204/304 and void-response empty-body lowering with no body `content-type`, route-param JSON object lowering such as `{ id: @param.id }`, query-param JSON object lowering such as `{ q: @query.q }`, route/query numeric-cast JSON object lowering such as `{ id: @param.id as int }` or `{ page: @query.page as float }`, route/query int/float add/sub/mul/div/rem/pow literal and captured-operand lowering such as `{ next: (@param.id as int) + 1 }`, `{ prev: (@query.page as int) - 1 }`, `{ doubled: (@param.id as int) * 2 }`, `{ half: (@query.page as int) / 2 }`, `{ parity: (@param.id as int) % 2 }`, `{ shifted: (@param.id as int) + (@param.offset as int) }`, `{ discounted: (@param.price as float) * 0.5 }`, `{ taxed: (@param.price as float) + (@param.tax as float) }`, or `{ total: (@query.amount as float) * (@query.quantity as float) }`, request-body JSON passthrough such as `{ received: @body }`, request-body field JSON object lowering such as `{ handle: @body.handle }`, request-body joined string interpolation such as `{ label: "{@body.first}-{@query.suffix}" }`, request-body field numeric-cast JSON object lowering such as `{ quantity: @body.quantity as int }` or `{ amount: @body.amount as float }`, request-body int add/sub/mul/div/rem lowering such as `{ quantity: (@body.quantity as int) + 1 }`, `{ due: (@body.total as int) - (@body.discount as int) }`, `{ cents: (@body.quantity as int) * 100 }`, `{ share: (@body.total as int) / (@body.parts as int) }`, or `{ remainder: (@body.total as int) % (@body.parts as int) }`, request-body numeric field multiplication such as `{ total: (@body.quantity as int) * (@body.unit_price as int) }` or `{ total: (@body.price as float) * (@body.quantity as float) }`, mixed static/domain/arithmetic JSON object lowering such as `{ err: "missing", sku: @body.sku }` or `{ kind: "calc", next_id: (@param.id as int) + 1 }`, simple request-body, route-param, query-param, cross-source, or numeric comparison guard dispatch such as `if @body.sku == "" { @respond ... }`, `if @body.token == @query.token { @respond ... }`, `if (@body.quantity as int) > 0 { @respond ... }`, `if (@body.quantity as int) <= (@body.stock as int) { @respond ... }`, `if (@body.quantity as int) <= (@body.stock as int) { @respond ... } else { @respond ... }`, `if (@body.amount as float) > 0.0 { @respond ... }`, `if (@body.amount as float) <= (@query.limit as float) { @respond ... }`, `if @param.kind == "sale" { @respond ... }`, or `if @query.mode != "compact" { @respond ... }`, dynamic body-lowering 501 placeholder, and router dispatch contract carrying matched route origin, first response origin, route params, decoded query params, request body, and decoded body fields. The launcher serves route artifacts whose single-response, simple guarded multi-response, or single `if/else` response pair handlers are fully native-lowered through a dependency-free `std::net::TcpListener` HTTP/1 loop and generated router/handler code; artifacts with DB, unsupported arithmetic expressions outside the supported request-body/route/query slices, unsupported casts, or otherwise unsupported dynamic route bodies keep a reference `orv run-artifact` fallback so full reference behavior remains deployable until broader dynamic native lowering exists. `deploy/README.md` documents the generated launcher path with `cargo build --manifest-path server/native/Cargo.toml --release` and `ORV_BUILD_DIR=. ./server/native/target/release/orv-native-server`; running from that generated path also infers the build directory, while `ORV_BUILD_DIR` remains an explicit override. The shop starter README emits the same workflow with `dist`-prefixed paths. `deploy/manifest.json` references the plans through `server.native_plan`, `server.native_runtime_image_plan`, `server.native_routes_source`, `server.native_router_source`, and `server.native_handlers_source`; `orv verify-build` checks that the plan/package/source/routes-source/router-source/handler-source/commands/image contract still match the server runtime artifact, and reveal/editor/LSP production payloads expose the matching native server target, build/run/image commands, route/router/handler source summary, and runtime image plan summary for route origins.

Static int `pow` operands outside the runtime-accepted exponent range `0..=63` stay on the generated native route path and return the same native arithmetic-failure class instead of forcing a reference bridge fallback.

Single `else if` response chains whose guard/body slices stay inside the same native-lowered request-body/route/query feature set are also direct-lowered; the generated handler emits ordered guarded returns and a final unconditional response.

The same chain shape is supported when the final unconditional `@respond` is a following route-handler statement instead of the last `else` branch.

`else { if ... }` block wrappers are normalized to the same ordered response chain when the block contains only that nested `if`.

Route path-param matching now recognizes static suffix segments such as `:userId.ics`; analyzer diagnostics reject `@param.<name>` references not declared by the current route path.

Request-body scaled captured arithmetic such as `{ quantity: (@body.quantity as int) + ((@body.bonus as int) * 2) }`, `{ quantity: ((@body.bonus as int) * 2) + (@body.quantity as int) }`, `{ quantity: (@body.quantity as int) - ((@body.bonus as int) * 2) }`, `{ quantity: ((@body.bonus as int) * 2) - (@body.quantity as int) }`, `{ cents: (@body.quantity as int) * ((@body.unit_price as int) * 100) }`, `{ unit: (@body.total as int) / ((@body.parts as int) * 100) }`, or `{ remainder: (@body.total as int) % ((@body.parts as int) * 10) }` is direct-lowered as `add_scaled`/`sub_scaled`/`rsub_scaled`/`mul_scaled`/`div_scaled`/`rdiv_scaled`/`rem_scaled`/`rrem_scaled` native metadata. Generated handlers parse both captured operands, checked-multiply the scaled operand, then checked-add/sub/mul/div/rem the result so these guarded commerce-style routes no longer require the reference bridge. Scaled captured comparisons in response fields and route guards, such as `{ available: (@body.quantity as int) <= ((@body.stock as int) * 10) }`, `{ covered: ((@body.minimum as int) * 100) <= (@body.total as int) }`, and `if (@body.quantity as int) <= ((@body.stock as int) * 10) { @respond ... }`, are also direct-lowered with checked scale multiplication before comparing.

Request-body captured product arithmetic/comparison such as `{ total: (@body.fee as int) + ((@body.quantity as int) * (@body.unit_price as int)) }`, `{ total: (@body.fee as int) - ((@body.quantity as int) * (@body.unit_price as int)) }`, `{ total: ((@body.quantity as int) * (@body.unit_price as int)) - (@body.fee as int) }`, `{ bundles: (@body.total as int) / ((@body.quantity as int) * (@body.unit_price as int)) }`, `{ remainder: ((@body.quantity as int) * (@body.unit_price as int)) % (@body.total as int) }`, `{ total: ((@body.quantity as int) * (@body.unit_price as int)) + 25 }`, `{ remaining: 1000 - ((@body.quantity as int) * (@body.unit_price as int)) }`, `{ covered: ((@body.quantity as int) * (@body.unit_price as int)) <= 1000 }`, `{ covered: (@body.total as int) <= ((@body.quantity as int) * (@body.unit_price as int)) }`, `if (@body.total as int) <= ((@body.quantity as int) * (@body.unit_price as int)) { @respond ... }`, or `if ((@body.quantity as int) * (@body.unit_price as int)) <= 1000 { @respond ... }` is direct-lowered as `add_product`/`sub_product`/`rsub_product`/`mul_product`/`div_product`/`rdiv_product`/`rem_product`/`rrem_product`, `*_product_static`, `*_product` comparison metadata, or condition secondary operand metadata. Generated handlers parse captured product operands, checked-multiply them, then checked arithmetic or compare against the base captured field or static operand so common cart-total and discount routes avoid the reference bridge.

Scaled-product response fields and guards such as `{ total: (@body.base as int) + (((@body.quantity as int) * (@body.unit_price as int)) * 100) }`, `{ total: (((@body.quantity as int) * (@body.unit_price as int)) * 100) + (@body.base as int) }`, `{ total: (@body.base as float) + (((@body.price as float) * (@body.quantity as float)) * 0.5) }`, `{ covered: (@body.total as int) <= (((@body.quantity as int) * (@body.unit_price as int)) * 100) }`, and `if (@body.total as int) <= (((@body.quantity as int) * (@body.unit_price as int)) * 100) { @respond ... }` are direct-lowered as `*_scaled_product` response metadata or condition `operand_scale_json` plus secondary operand metadata. The base captured field stays primary, `operand_json`/`operand_scale_json` carries the static scale, and `operand_*` plus `secondary_operand_*` carry the two product factors; generated handlers compute the product and scale with checked int arithmetic or finite float arithmetic before applying the response operation or branch.

Product-product response fields such as `{ total: ((@body.quantity as int) * (@body.unit_price as int)) + ((@body.fee_units as int) * (@body.fee_value as int)) }` and `{ covered: ((@body.quantity as int) * (@body.unit_price as int)) <= ((@body.stock as int) * (@body.reserve_price as int)) }` are direct-lowered as `*_product_product` metadata. The left product keeps the primary captured field plus `operand_*`, the right product uses `secondary_operand_*` and `tertiary_operand_*`, and generated handlers compute both products before checked arithmetic or comparison.

Captured triple-product response fields such as `{ total: (@body.base as int) + (((@body.quantity as int) * (@body.unit_price as int)) * (@body.bundle_count as int)) }`, `{ total: (((@body.quantity as int) * (@body.unit_price as int)) * (@body.bundle_count as int)) + (@body.base as int) }`, `{ total: (@body.base as float) + (((@body.price as float) * (@body.quantity as float)) * (@body.multiplier as float)) }`, `{ covered: (@body.total as int) <= (((@body.quantity as int) * (@body.unit_price as int)) * (@body.bundle_count as int)) }`, or the equivalent left-side comparison are direct-lowered as `*_triple_product` arithmetic/comparison metadata. The base captured field stays primary while the three product factors use `operand_*`, `secondary_operand_*`, and `tertiary_operand_*`; generated handlers parse all factors, validate the triple product, then apply checked int arithmetic/comparison or finite float arithmetic/comparison before writing JSON. Matching request-body guards such as `if (@body.total as int) <= (((@body.quantity as int) * (@body.unit_price as int)) * (@body.bundle_count as int)) { @respond ... }` use the same operand contract and stay on the direct native dispatch path.

Product-product guards such as `if ((@body.quantity as int) * (@body.unit_price as int)) <= ((@body.stock as int) * (@body.reserve_price as int)) { @respond ... }` use the same condition `operand_*`/`secondary_operand_*`/`tertiary_operand_*` contract and stay on the direct native dispatch path.

When a server artifact contains static persistence paths such as `@db.wal("data/app.wal.jsonl")`, `@db.connect "file://data/app.wal.jsonl"`, or `@db.connect "sqlite://data/shop.sqlite"`, the deploy manifest and container contract record those WAL/SQLite paths and the generated Compose file mounts the parent directory, for example `../data:/app/data`. `orv run-build <dir>` resolves relative DB, WAL, `@serve`, `@fs`, and file-backed commerce adapter paths against `<dir>` so local production smoke runs write into the build directory instead of the caller's shell cwd. Env-configured DB adapters such as `@db.connect(@env.SHOP_DATABASE_URL ?? "sqlite://data/shop.sqlite")` record the env/default pair in `db_env`, emit Compose environment entries, write matching `deploy/env.example` assignments, and still record the default SQLite/file path for volume mounting. PostgreSQL/MySQL defaults are recorded as external `db_endpoints`, listed without Compose volumes, and mirrored into `deploy/db-adapters.json` with provider, env/default, endpoint, current adapter status, reference query method contract, checked bridge request body, bounded transient retry policy, provider-specific and generic bridge endpoint/auth env knobs, and a `source_origin_id` pointing back to the `@db.connect` call so operators and reveal surfaces can trace each adapter contract to source. Runtime handles still fail query/mutation methods by default, but configured `ORV_DB_ADAPTER_POSTGRES_ENDPOINT`, `ORV_DB_ADAPTER_MYSQL_ENDPOINT`, or `ORV_DB_ADAPTER_ENDPOINT` values switch them to a checked `http-json-v1` bridge that POSTs `{"kind":"orv.db.adapter","contract":"http-json-v1","provider","url","method","args"}` and returns the bridge JSON response as an ORV value after up to three transient retry attempts; provider-specific or generic `ORV_DB_ADAPTER_*_AUTH_TOKEN` values are sent as bearer tokens. Env-only DB adapters such as `@db.connect(@env.SHOP_DATABASE_URL)` are recorded as required deployment envs, and `orv deploy-env-check <build-dir>` fails before launch when those required DB env values are unset or blank. Static PostgreSQL/MySQL adapters also make either provider-specific or generic bridge endpoint env required in preflight, while bridge auth token envs remain optional and can also fall back to the generic auth token env. Generated deploy smoke tests check `deploy/db-adapters.json` and POST a safe `schema` probe to each provider-specific or generic configured bridge endpoint before route smoke proceeds. File-backed commerce adapters are recorded as `record_paths` and share the same Compose parent-directory mount contract. Static HTTP commerce adapters are recorded as `commerce_endpoints`, documented in the generated runbook as external dependencies, and intentionally do not create Compose volumes. Env-configured commerce adapters record the env/default pair in `commerce_env` and emit Compose environment entries like `PAYMENT_ADAPTER_URL: "${PAYMENT_ADAPTER_URL:-http://payments.internal/capture}"` plus matching `deploy/env.example` assignments; HTTP defaults are also recorded in `commerce_endpoints`, while file defaults remain `record_paths`. `deploy/commerce-adapters.json` records each payment/shipping adapter by kind, mode, env/default, endpoint or record path, source `@payment.connect`/`@shipping.connect` origin id, and the reference HTTP request body contract (`{"kind","payload"}`) so operators can wire external adapter services without reading source. Provider-mode adapters add a checked `provider_env` contract; Stripe payment handles expose `STRIPE_API_ENDPOINT`, `STRIPE_SECRET_KEY`, and `STRIPE_WEBHOOK_SECRET`, carrier shipping handles expose `CARRIER_API_ENDPOINT`, `CARRIER_API_KEY`, and `CARRIER_WEBHOOK_SECRET`, and Compose/env.example/runbook forward those variables with required/optional labels. `deploy/preflight.json` mirrors the verified commands, including source-bundled `orv editor run-debug . --control next`, trace-enabled `orv run-build . --trace deploy/request-trace.json`, linked artifacts, runtime/security features, persistence contract, required/optional env contract, and the 5-hour shop benchmark gate with success criteria, time budget, and data-to-record fields; the same deploy env check reads that preflight artifact plus DB/commerce adapter artifacts and fails before launch when required DB, DB bridge, provider, or env-only adapter values are missing while allowing optional auth endpoint/webhook envs to remain unset. Provider capture/booking responses report `configured`/`missing` credential status metadata without returning secret values; when `STRIPE_API_ENDPOINT` or `CARRIER_API_ENDPOINT` is configured, provider capture/booking POST checked JSON to that endpoint with bearer credentials, stable idempotency keys, and bounded transient retry, then merge the provider JSON response into the reference result. Stripe payment handles also expose `payments.verifyWebhook({ payload, signature })`, which checks `STRIPE_WEBHOOK_SECRET` against Stripe-style `t=...,v1=...` HMAC-SHA256 signatures, rejects stale/future timestamps outside `STRIPE_WEBHOOK_TOLERANCE_SECONDS` or the 300-second default, and returns `payment.webhook` status metadata without returning secret values or webhook payloads. The shop scaffold hosts `POST /webhooks/stripe`, passes `@request.rawBody` and `@header["stripe-signature"]` through that verifier, persists `WebhookEvent.eventId` rows for operator visibility, returns the existing row instead of inserting duplicates for repeated provider/eventId deliveries, and applies reference Payment/Order status reconciliation when verified webhook JSON carries `orderId`, `paymentStatus`, and `orderStatus`. Production provider SDK matrix/credential rotation hardening remains future work. `orv reveal`, `orv editor reveal`, and `orv lsp reveal` expose the same DB and commerce adapter contracts in the production payload; DB and commerce reveal payloads also include `matched_adapters` for the selected source origin when it is the adapter call or a contained origin, plus `source_reveal_commands` so native hosts can jump from each adapter contract back through `orv editor reveal <build-dir> <origin-id>`.

`deploy/benchmark-evidence.json` is generated from the preflight contract, records the preflight hash, and exposes editable evidence slots for task timing, docs/help lookups, compiler/runtime errors, first-error-to-fix time, manual config edits, smoke-test output, required smoke-output markers, 2-3 person participant-run metadata, failure classification, and participant notes. `deploy/preflight.json` names `orv editor run-debug . --control next`, `orv benchmark-report .`, and `orv benchmark-report . --require-pass` in its checked command list, names `deploy/smoke-output.txt` as the generated smoke output artifact, and carries a checked `smoke_output_contract` with the same required marker list. Generated smoke tests write that file on success with a compact summary of the checked build directory, base URL, graph contract, DAP production-summary gate, DAP source-bundle panel gate, route count, trace-stream request flag, and client bundle paths when present, so runbook, evidence, and CI gating share the same benchmark report entrypoint without requiring operators to copy smoke stdout by hand. The generated deploy runbook lists the same required smoke-output markers so operators can inspect the benchmark contract without opening JSON artifacts. `orv verify-build` checks the benchmark/preflight/evidence/runbook schema, including the required marker list with `dap_source_bundle`, while allowing recorded measurement values to be filled later. `orv benchmark-report <dir>` parses that smoke summary into `data.smoke_test_summary`, carries the required marker list beside any missing markers and the top-level smoke output contract, marks weak or copied-only smoke strings incomplete when pass/graph/DAP/route markers are missing, summarizes the recorded evidence as pass/fail/incomplete JSON, and `--require-pass` turns that report into a CI gate. Reveal/editor/LSP production payloads expose the benchmark evidence summary and preflight smoke output contract through the preflight target, including report status, recorded/missing task counts, missing data names/count, smoke output source, required marker list, parsed smoke summary, elapsed total, and time-over-limit status. They also carry the same production summary counters used by editor export/native-host payloads, including graph-contract counts and preflight smoke evidence present/missing/missing-marker counts.

`source-bundle.json` records source path/content/hash snapshots so reveal, LSP reveal, artifact verification, artifact reanalysis, and reference artifact execution do not depend on the original source files. `orv verify-build` checks that `project-graph.json` file nodes match this bundle, that its embedded semantic origin-map/edges/origin-links still match `origin-map.json`, that server runtime and deploy source snapshots still match this build-level bundle, and that server route/listen/response origin ids plus DB/commerce adapter source origin ids resolve through the expected `origin-map.json` entries before smoke-test or reveal tooling trusts production metadata.

For interactive client entries, `client/manifest.json` binds the page, reactive plan path/hash, JS loader path/hash, WASM module path/hash, source bundle hash, WASM exports, initial render metadata, runtime features, a reactive capability inventory derived from `client/reactive-plan.json`, and dynamic-client-codegen blockers into one checked artifact. `client/reactive-plan.json` records source-backed `let sig` origins, top-level signal initial state metadata, the initial-render binding, per-signal `signal_state` bindings to `client/app.js`, source-backed `signal_text` bindings to initial DOM text slots plus single- and multi-signal interpolated text templates with explicit `state_keys`/`sources`, single-signal conditional text branches, source-backed `signal_attr` bindings to initial DOM attributes/properties, single-signal boolean conditional attributes, and single-signal literal comparison conditional attributes, source-backed `signal_event` bindings for simple event-time signal assignments, boolean toggles, form input target-value updates, checkbox target-checked updates, and numeric target-value casts, source bundle hash, and `reactive-dom-diff` blocker for the future full DOM-diff codegen pass. Both client artifacts include structured `blockers` entries with `id`, `artifact`, and `reason` fields aligned with `blocked_by`, so reveal/editor surfaces can explain remaining client codegen blockers without hardcoding strings. The generated `client/app.js` now embeds the same reactive plan plus a build-time plan hash, validates that embedded plan before falling back to the external `client/reactive-plan.json`, verifies source bundle hash/count metadata from the bootstrap instead of fetching source text in the browser, rejects schema/hash/WASM/export/reactive-plan/signal-state/signal-text/signal-attr/signal-event mismatches, mounts the decoded full-document initial render into `#orv-root` as body content, creates the initial client reactive state, binds matching text slots/templates/conditions and attributes/templates/conditions with cursor-stable duplicate initial slot matching, binds event handlers, exposes `__ORV_SET_SIGNAL__`, and then records the resolved manifest URL, reactive signal count, signal-state binding count, DOM binding count, attr binding count, event binding count, and state hash on the client root. `orv verify-build` validates the manifest and reactive plan against the generated page/loader/WASM/source bundle plus structured blocker metadata, manifest loader/reactive-plan hashes, and manifest capability counts/surfaces/event action inventory, prod `deploy/manifest.json` references the manifest as `client.manifest` and the plan as `client.reactive_plan`, and reveal/editor/LSP production payloads expose both client contracts for client origins.

Reveal/editor/LSP client-origin production payloads also forward the manifest capability inventory, so native editor and LSP surfaces can show supported client runtime surfaces without rereading `client/manifest.json`.
Production deploy manifests mirror that same inventory as `client.capabilities` plus `client.blocked_by`/`client.blockers`, and `orv verify-build` rejects deploy/client manifest drift for reactive-plan path, capability, and blocker metadata.
For server+client production builds, static/client initial render filters out top-level `@server` declarations so build does not bind the production listen socket, the generated deploy runbook documents the client manifest/reactive-plan/page/loader/WASM paths, runtime, capability surfaces, and blocker summary, and the generated deploy smoke test changes to its own build directory before checking route reachability, exact `x-orv-origin-id` route headers from the server artifact, exact `x-orv-response-origin-id` headers for covered routes with one unambiguous response origin, route/response/DB/commerce/client source reveal through `orv reveal`, `orv editor reveal`, and `orv lsp reveal`, source-bundled DAP production summaries through `orv editor run-debug . --control next`, shop checkout/admin response markers, and client files plus page marker, loader reference, manifest path/hash/source/capability inventory, reactive-plan schema markers, loader bootstrap strings, and client target/manifest/capability summary counters. Shop deploy smoke also checks that CLI/editor/LSP preflight reveal payloads expose the benchmark smoke summary contract and the production summary smoke-evidence counters. When the server is started with trace capture, `ORV_SMOKE_TRACE_STREAM=1 ./deploy/smoke-test.sh` also captures `/__orv/trace/events` and validates it through `orv editor trace-stream`. Reference and native HTTP responses also expose `x-orv-response-origin-id` when an executed `@respond` origin is known, and request trace JSON carries the same `response_origin_id` for editor response reveal. `orv verify-build` rejects smoke-test drift from that client bundle, route-origin/response-origin contract, DAP production-summary gate, and CLI/editor/LSP reveal coverage.

## DB Operations

Runtime `@db` currently uses an in-memory execution model with explicit JSON snapshot and WAL APIs: `@db.save/load`, `@db.wal(path)`, `@db.checkpoint()`, `@db.savepoint()`, and `@db.rollback(point)`. `@server` boot body DB setup is carried into route handlers, so a server-level SQLite handle such as `let shopdb = @db.connect(@env.SHOP_DATABASE_URL ?? "sqlite://data/shop.sqlite")` can be captured by routes and persist product/member/order/payment/shipment mutations across runs. The shop scaffold stores member signup rows, login session rows, cart item rows, and AuditEvent rows for product/member/session/cart/order/checkout/payment/shipping/webhook operations in the same SQLite handle, `GET /catalog` renders persisted Product rows for shoppers, `GET /cart` renders persisted CartItem rows, `GET /account/sessions` renders active Session rows for browser account inspection, `GET /admin/summary` uses the handle's `count` aggregation to expose product/member/order/payment/shipment/webhook/audit row counts for operators, and the admin read-model routes render persisted Product, Order, Payment, Shipment, WebhookEvent, and AuditEvent rows for browser inspection. `@db.connect` accepts the reference `memory://` adapter, a local WAL-backed `file://path` adapter, and a SQLite-backed `sqlite://path` adapter that stores ORV table metadata plus row JSON in a real SQLite file while preserving current query semantics. PostgreSQL/MySQL adapter URLs return external status handles until real provider implementations exist; those handles expose `adapterStatus`, `url`, provider, and `runtime.queryMethods` metadata. Query/mutation methods fail explicitly by default, or POST checked `http-json-v1` JSON to a configured DB adapter bridge endpoint and return the bridge JSON response. Build/prod deploy artifacts record those URLs as external `db_endpoints` without creating Compose volumes and emit a checked `deploy/db-adapters.json` operator contract. Build/deploy/server runtime artifacts include the `db_adapter` runtime feature when source uses `@db.connect`. Production deploy persistence scans `@db.wal("relative/path.jsonl")`, `@db.connect "file://relative/path.jsonl"`, and `@db.connect "sqlite://relative/path.sqlite"`, records SQLite files as `db_paths`, records env-configured DB adapters as `db_env`, and mounts eligible parent directories into Compose.

Runtime commerce adapters currently provide local reference handles: `@payment.connect("test://local").capture(...)` returns captured payment metadata and `@shipping.connect("test://local").book(...)` returns shipment booking metadata. The shop scaffold uses env-overridable local handles, `@payment.connect(@env.PAYMENT_ADAPTER_URL ?? "file://data/payments.jsonl")` and `@shipping.connect(@env.SHIPPING_ADAPTER_URL ?? "file://data/shipments.jsonl")`, so unset env defaults append and sync local JSONL records before matching DB rows are persisted, while deploy operators can point the same source at external HTTP adapters. `http://host/path` handles are reference external adapters: capture/book calls POST `{"kind","payload"}` JSON to the configured HTTP endpoint and return its JSON response as an ORV value. Prod deploy persistence records relative file adapter paths as `record_paths` and mounts their parent directories into Compose; HTTP adapter URLs are recorded as `commerce_endpoints` in deploy/container persistence and listed in the runbook without adding volumes. When the adapter URL comes from `@env.NAME ?? "default-url"`, deploy/container persistence also records `commerce_env` with the env name and default URL, the generated Compose file forwards that env with the default fallback, and the runbook lists the override knob. `deploy/commerce-adapters.json` mirrors those adapters into a checked operator contract for payment capture and shipping booking wiring. Builds that use these connections record `payment_adapter` and `shipping_adapter` runtime features in build, server runtime, and deploy artifacts. Provider-specific reference handles are available for `@payment.connect("stripe://...")` and `@shipping.connect("carrier://...")`; prod artifacts record them as `mode: "provider"` with `provider` names instead of pretending they are HTTP endpoints or local record logs, provider-mode artifacts expose required/optional credential env contracts for Stripe and carrier handles, runtime provider responses expose credential status metadata without leaking the configured values, and Stripe handles can verify reference webhook signatures with `payments.verifyWebhook({ payload, signature })`. The shop scaffold exposes that as `POST /webhooks/stripe`, persists idempotent `WebhookEvent` rows keyed by event id, and includes the persisted count in `GET /admin/summary`.

The reference HTTP server sets a built-in member session cookie for successful `POST /members/login` responses that return `session.id`: `orv_session=<id>; Path=/; Max-Age=86400; HttpOnly; SameSite=Lax; Secure`. If the response session carries `role`, it also emits `orv_session_role=<role>` with the same cookie defaults. String session ids and roles must already be cookie-safe ASCII scalars; unsafe values are not emitted. The reference `hash` namespace supports `hash.sha256`, Argon2 `hash.password`, and `hash.verify`; the shop scaffold stores only `passwordHash` values and rejects login when verification fails. Route handlers can use `@session.id`/`@session.role` to read those cookies, `@session required` to return `401 { err: "session_required" }`, and `@Auth required role="admin"` to return `401 { err: "auth_required" }` without a session or `403 { err: "role_required" }` for a mismatched role before later handler statements run. HTML GET responses receive a reference `orv_csrf=orv-reference-csrf` cookie, and route handlers can use `@csrf` to require a matching `x-csrf-token`, `x-orv-csrf-token`, `_csrf`, or `csrf` submitted token; mismatch returns `403 { err: "csrf_token_required" }` before later handler statements run. `@csrf exempt` records an intentional source-backed exemption and lets the request continue without a token. The shop product, member, login, order, cart, checkout, payment, and shipment browser mutation forms include the hidden `_csrf` token and their matching POST routes use `@csrf`; `POST /webhooks/stripe` remains signature-verified instead of CSRF-gated because it is a provider callback endpoint. The server also applies built-in one-minute route limits to the MVP shop mutation hotspots: `POST /members/login` and `POST /checkout` allow 10 requests per client IP, and `POST /webhooks/stripe` allows 60 requests per client IP. Explicit `@rateLimit` policies can override those limits, use request keys such as `@body.memberId`/`@query.memberId`/`@session.id`, or use `exempt` to remove the built-in limit for a route. Exceeded requests return `429 Too Many Requests` before handler execution.

The shop `POST /cart/items` route persists validated member/product cart rows for the browser `GET /cart` view, and `POST /checkout` composes member lookup, stock reservation, payment capture, shipment booking, and final order status update in one browser/API path while preserving the separate member/order/payment/shipment routes for explicit operations. Checkout now wraps the stock decrement and initial order creation in the captured `shopdb.transaction(...)` boundary before provider calls, so local DB rollback covers the reservation/order pair when that phase fails. If payment capture succeeds but shipment booking or shipment persistence fails, checkout updates the order to `payment_captured_pending_shipment`, records `checkout.compensation_required` in `AuditEvent`, and returns a 202 response with the pending order/payment context for operator follow-up.

CLI DB commands provide schema/data dry-run, drift verification, apply/migrate with history, rollback, local backup/restore, hash-verified WAL recovery, archive manifest generation, `file://` archive target copies, HTTP `POST` archive target uploads plus HTTP `GET` restore fallback, S3-compatible `s3://` archive targets through `ORV_DB_ARCHIVE_S3_ENDPOINT` with optional bearer auth or `ORV_DB_ARCHIVE_S3_AUTH=aws-sigv4`, bounded retry for transient HTTP/S3-compatible upload/download failures via `ORV_DB_ARCHIVE_HTTP_RETRY_ATTEMPTS`, manifest-relative source WAL resolution, raw-WAL/archive point-in-time restore, crash/recovery matrix reporting via `orv db crash-matrix --out <report.json>`, and history squash.
