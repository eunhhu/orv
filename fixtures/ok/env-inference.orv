// Environment variable inference and coercion
@server {
  let port: i32 = @env PORT
  let secret = @env SECRET
  let debug: bool = @env DEBUG

  @listen port

  @route GET /api/config {
    @respond 200 { "debug": debug }
  }
}
