// Pattern matching with various pattern types
let x = 5
let label = when x {
  0 -> "zero"
  1 | 2 | 3 -> "small"
  _ -> "large"
}

enum Color {
  Red
  Green
  Blue
}

let name = when Color.Red {
  Color.Red -> "red"
  Color.Green -> "green"
  Color.Blue -> "blue"
}
