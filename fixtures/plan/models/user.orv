// status: planned-only
// contract: non-binding
// milestone: M4+
// purpose: module-system support for plan fixtures, not an MVP compile contract

pub struct User {
  name: string
  age: int
}

pub function createUser(input: {name: string, email: string}): User -> {
  { name: input.name, age: 0, email: input.email }
}
