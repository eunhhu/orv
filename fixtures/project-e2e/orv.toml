[manifest]
name = "project-e2e"
version = "0.1.0"
description = "A project for end-to-end testing"
entry = "src/main.orv"

[scripts]
build = "orv build" # 'orv build' compiles the entry file
dev = "orv src/main.orv --hot" # 'orv dev' runs the entry file

[dependencies]
# Add dependencies here

