import ...lib.db.db

pub define List() -> @route GET /list {
  let skip = @query skip ?? 0
  let limit = @query limit ?? 50
  let users = @db find User {
    @skip skip
    @limit limit
  }
  @respond 200 { data: users }
}